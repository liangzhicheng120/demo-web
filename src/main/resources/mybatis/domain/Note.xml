<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xinrui.demo.dao.NoteDao">

	<select id="getAllByPage" resultType="Note">
		SELECT
		ID,
		CONTENT,
		KEYWORD,
		POSTTIME,
		TITLE,
		NCLASS
		FROM
		note
		<where>
			1=1
			<if test="keyword != null and !&quot;&quot;.equals(keyword.trim())">
				AND KEYWORD LIKE CONCAT('%', #{keyword}, '%')
			</if>
			<if test="nclass != null and !&quot;&quot;.equals(nclass.trim())">
				AND NCLASS LIKE CONCAT('%', #{nclass}, '%')
			</if>
		</where>
	</select>

	<delete id="delete" parameterType="Note">
		DELETE FROM
		note
		WHERE id =#{id}
	</delete>

	<delete id="batchDelete" parameterType="java.util.List">
		DELETE
		FROM
		note
		WHERE
		id
		IN
		(
		<foreach collection="list" item="id" separator=",">
			#{id}
		</foreach>
		)
	</delete>

	<insert id="save" parameterType="Note">
		INSERT INTO
		note
		(
			CONTENT,
			KEYWORD,
			POSTTIME,
			AID,
			TITLE,
			NCLASS
		)
		VALUES
		(
			#{content},
			#{keyword},
			now(),
			#{aid},
			#{title},
			#{nclass}
		)
	</insert>

</mapper>